{
  "name": "clean-architecture-backend",
  "version": "1.0.0",
  "description": "study clean-architecture",
  "main": "handler.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild handler.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/handler.mjs"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.148",
    "esbuild": "^0.25.1"
  }
}
